# Staging profile — cloud deployment (Render / Railway)
# OTP logged to server logs; mock payments. Use real prod profile for MSG91 + Razorpay.

sms.provider=console
payment.provider=mock

seed.admin.phone=9000000001

# Override via environment variables on the host:
#   SPRING_DATA_MONGODB_URI=mongodb+srv://...
#   JWT_SECRET=<long-random-secret-min-256-bits>
#   SPRING_PROFILES_ACTIVE=staging
