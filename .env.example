# Copy to .env for local reference only — never commit .env
SPRING_PROFILES_ACTIVE=dev
SPRING_DATA_MONGODB_URI=mongodb://localhost:27017/dr20db
JWT_SECRET=change-me-min-256-bits-long-enough-for-jwt-signing!!
PORT=8081

# Staging / production (set on Render/Railway dashboard, not in git)
# SPRING_PROFILES_ACTIVE=staging
# SPRING_DATA_MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/dr20db
